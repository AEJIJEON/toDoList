* Image() constructor -> creates a nuew HTMLImageElement instance.
(functionally equivalent to document.createElement('img'))
const image = new Image();
image.src = "path......"

-> image "loaded" listener 작동 x (사진이 api에서 나온게 아니라..)
(api에서 이 일을 하고 있다면 "loaded"를 listen하는것이 필요함..)

object만들 때 변수 이름이랑 key 이름 같게 저장할 때에는
{
value1,
value2
}
same as
{
value1: value1,
value2: value2
}

js는 웹사이트로 request를 보내고 응답을 통해 데이터를 얻을 수 있음
새로고침 없이 데이터 가져올 수 있음(by fetch)
-> network패널에 request  등록됨(필요한 데이터)
fetch될 때까지 아래 코드 수행되지 않기 위해서
fetch{}.then{ftn} ftn 호출
